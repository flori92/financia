[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start:prod"
# healthcheckPath = "/api/v1/health"  # Temporairement désactivé
# healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"

# Configuration explicite du service
[service]
name = "bms-api-gateway"
