// Index des hooks pour le BMS
export { useAuth } from './useAuth';
export { usePermissions, ProtectedComponent, useRequireAuth } from './usePermissions';
export { UserRole, type User } from '../types/auth';
